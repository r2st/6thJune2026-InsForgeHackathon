{
  "name": "@hush/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Hush edge functions — runs on InsForge.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no linter yet'",
    "test": "echo 'no tests yet'",
    "deploy": "insforge functions deploy ingest && insforge functions deploy fix-trigger"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
