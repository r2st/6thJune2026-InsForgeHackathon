{
  "name": "@hush/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Hush edge functions — runs on InsForge.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no linter yet'",
    "test": "vitest run",
    "deploy": "node ../scripts/deploy-insforge-functions.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": ">=0.40.0",
    "@insforge/sdk": "^1.0.0",
    "@llm-guardrails/core": "^0.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
