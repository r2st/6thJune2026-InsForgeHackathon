{
  "name": "@hush/functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Hush edge functions — runs on InsForge.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no linter yet'",
    "test": "vitest run",
    "deploy": "npx @insforge/cli functions deploy ingest --file ./ingest.ts && npx @insforge/cli functions deploy fix-trigger --file ./fix-trigger.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": ">=0.40.0",
    "@insforge/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
