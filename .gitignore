.DS_Store

# Environment / secrets — never commit any .env variant.
# `.env.example` is the only exception (committed by name below).
.env
.env.*
!.env.example

# Local ticketing system DB
tickets.db
tickets.db-wal
tickets.db-shm

# Python
__pycache__/
*.pyc

# Opsera security scan artifacts
gitleaks-report.json
gitleaks-stderr.txt
semgrep-report.json
semgrep-stderr.txt
grype-report.json
grype-stderr.txt
checkov-report.json
checkov-stderr.txt
hadolint-report.json
hadolint-stderr.txt
npm-audit.json
npm-audit-stderr.txt
package-leakage-report.json
